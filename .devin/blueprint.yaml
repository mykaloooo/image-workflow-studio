maintenance: |
  pip install -r backend/requirements.txt
  (cd frontend && npm install)
knowledge:
  - name: run
    contents: |
      (cd backend && python app.py)
  - name: dev
    contents: |
      (cd frontend && npm run dev)
  - name: build
    contents: |
      (cd frontend && npm run build)
  - name: note
    contents: |
      electron 目录依赖较重，本蓝图未安装，需要时本机另装。
