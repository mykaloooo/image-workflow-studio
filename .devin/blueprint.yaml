maintenance: |
  pip install Flask==3.0.0 Flask-CORS==4.0.0 Pillow==10.1.0 'google-genai>=1.60.0'
  (cd frontend && npm install)
knowledge:
  - name: run
    contents: |
      (cd backend && python app.py)
  - name: dev
    contents: |
      (cd frontend && npm run dev)
  - name: build
    contents: |
      (cd frontend && npm run build)
  - name: note
    contents: |
      backend/requirements.txt 里的 portfinder pin 无法解析，电子端 electron 目录按重依赖原则未安装。
