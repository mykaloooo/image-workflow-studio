# image-workflow-studio 项目规则

## Eagle 操作最高优先级规则

凡是涉及 Eagle 文件夹、子文件夹、导入、移动、删除、标签、注释、多文件夹关联的任务，必须先执行 `.windsurf/workflows/eagle-folder-ops.md` 的定位与验证分支。

### 禁止事项

- 禁止凭截图、文件夹名、图片名猜测目标位置。
- 禁止在未确认完整 Eagle 路径和 `folderId` 前导入图片。
- 禁止把原始素材文件夹、产品成品文件夹、`gen`、`淘汰 Bad` 混用。
- 禁止直接修改 Eagle library 里的 `metadata.json`、`tags.json`、`mtime.json`、`.info/metadata.json`。
- 禁止在未得到卡卡明确确认前删除文件夹、删除图片、批量移动、批量改标签。
- 禁止使用 HTTP `/api/item/update` 作为可靠的改名、改文件夹、改标签方案；必须验证实际结果。

### 必做流程

1. 先用 `GET http://localhost:41595/api/folder/list` 递归打印目标候选文件夹完整路径。
2. 如果同名或近似文件夹超过 1 个，必须列出候选路径和 `folderId`，让卡卡确认，或依据工作流内固定 ID 表确认。
3. 导入前必须明确目标语义：原始素材、产品最终版、临时 `gen`、淘汰版本、测试图。
4. 导入后必须用 `/api/item/info?id=...` 和 `/api/item/list?folderId=...` 双重验证。
5. 验证结果必须包含：item id、name、width、height、folders、目标完整路径。
6. 批量生图写回 Eagle 时，annotation 必须包含完整提示词；如果模型实际使用英文提示词，必须同时包含中文版对照提示词。
7. 如果 Eagle UI 看不到，以 API 验证为准先排查路径；不要立刻重复导入。

### 读取 Eagle API 的强规则（终端不卡死）

**本节为 Eagle 读取强约束，所有窗口、所有任务、所有 AI 都必须遵守。**

**绝对禁止**用以下方式读 Eagle API（会让 PowerShell/Cascade 终端长时间挂住等待输入）：

- ❌ `curl http://localhost:41595/...`（PowerShell 里 `curl` 是 `Invoke-WebRequest` 别名，遇到大 JSON 会卡）
- ❌ `Invoke-RestMethod` / `Invoke-WebRequest` 直接打印结果（中文+大 JSON 输出会卡终端）
- ❌ `curl.exe ... | python ...`（管道里把 1MB+ 的 JSON 作为 stdin 传给 Python，PowerShell 终端容易卡）
- ❌ 任何把 Eagle API 响应直接 echo 到终端的方式

**必须**用以下方式（标准模板，复制即用，不会卡终端）：

```powershell
# 步骤1: Python urllib 写入文件（永远不打印响应内容）
python -c "import urllib.request; open(r'D:\2026AI\image-workflow-studio\.runtime\_eagle_probe.json','wb').write(urllib.request.urlopen('http://localhost:41595/api/folder/list', timeout=10).read())"

# 步骤2: Python 单独读文件，只打印你需要的字段（控制输出量）
python -c "import json; d=json.load(open(r'D:\2026AI\image-workflow-studio\.runtime\_eagle_probe.json','r',encoding='utf-8')); print(len(d.get('data',[])), '个顶层文件夹')"
```

要点：

- 读 Eagle API 必须先写到 `.runtime\_eagle_probe.json`（或类似临时文件），再用 Python 读取所需字段。
- 任何打印必须有上限（推荐 `[:3000]` 或只打需要的几个 key）。
- `httpx` 在没装时会失败；urllib 是 Python 标准库，**永远可用**，优先用 urllib。
- 写操作（POST/PUT）也必须用 Python `urllib.request.Request` 或 `requests`/`httpx`，不要用 PowerShell `Invoke-RestMethod`。

### Eagle 改动必须先写本地，再写中央记忆

**中央记忆不可靠**（115 服务器宕机、跨设备同步延迟、向量检索漏召回），所以 Eagle 相关任何重要发现都必须先落本地，再考虑同步中央记忆。

**写入触发**（任何一条命中就必须写）：

- 发现 Eagle API 新行为、新字段、新边界。
- 踩到 Eagle 操作的坑（API 返回不一致、UI 与数据不同步、插件失效等）。
- 制定或修订 Eagle 操作规则。
- 工具/插件/queue 流程改动。
- 发现以前的结论错了，需要修正。

**必写位置**（按优先级）：

1. **`EAGLE_CHANGELOG.md`**（项目根目录，权威本地副本，新条目写在最上面）
2. 如涉及强约束 → 同步到 `.windsurfrules` 对应章节
3. 如涉及操作流程 → 同步到 `.windsurf/workflows/eagle-folder-ops.md` 对应章节
4. 如涉及读写代码 → 同步到 `EAGLE_FOLDER_GUIDE.md`
5. **最后**才写中央记忆（`mcp2_remember`，scope=team_shared），且本地路径必须放在记忆内容里

**禁止**：

- 禁止只写中央记忆不写本地。
- 禁止改了 Eagle 相关的 .windsurfrules / workflow / GUIDE 而不更新 `EAGLE_CHANGELOG.md`。
- 禁止跳过 `EAGLE_CHANGELOG.md` 直接补 workflow（必须先记录"为什么改"，再改 workflow）。

**条目格式**：详见 `EAGLE_CHANGELOG.md` 顶部"写入规则"。最低字段：日期、类型 `[规则]/[字段]/[踩坑]/[API]/[工具]/[决策]`、标题、详情（现象→原因→解决→验证）、同步位置清单。

### 文件夹颜色和图标字段（卡卡的语义标记层）

`/api/folder/list` 返回的每个文件夹节点可能带这两个字段，**卡卡用它们给 AI 下指令**，读取时必须保留并向卡卡汇报：

- **`iconColor`**：文件夹颜色标记。已观测取值：`red`（截图中 P1 红）、`blue`、`aqua`（截图中 P2 青色）。Eagle UI 还支持 yellow/green/purple/pink/orange。
- **`icon`**：文件夹符号标记。已观测取值：`upload`（截图中 P3 上传箭头）、`lightbulb`（灯泡）、`excalmation`（Eagle 自己拼错的 exclamation 感叹号，必须按这个错拼来匹配）。
- 两个字段可同时存在；缺失即"无标记/默认灰色文件夹"。

**任何时候列出 Eagle 文件夹给卡卡看，都必须把 `iconColor` 和 `icon` 一起带出来**，不要只给 name + id。语义对照表（red 代表什么、lightbulb 代表什么）由卡卡口头/记忆里指定，不要自己揣测，遇到没有约定的标记就直接列出原始值让卡卡解释。

### 沙发产品文件夹边界

- 父文件夹 `艾狄 沙发` 只放原始素材和产品子文件夹，不作为最终交付目录。
- 每个 `P*` 产品成品文件夹只放该产品最终版图片。
- `gen` 只放临时生成或插件批量生成回传，不等于成品交付。
- `淘汰 Bad` 只放淘汰、错误、迭代轨迹。
- 生成图如果产品结构不匹配参考图，不能放入产品最终版文件夹，应放 `淘汰 Bad` 或临时区。

### Eagle API / SDK 边界

- 插件环境内优先使用 Eagle SDK：`eagle.item.addFromPath(..., { folders: [folderId] })`，并用 `eagle.item.modify(id, { folders: [folderId] })` 兜底。
- 非插件环境 HTTP API 只能作为 fallback；`addFromPath` 可能异步，必须等待并复查。
- HTTP `/api/item/update` 历史上对 `folders` 不可靠，不能当作移动/多文件夹关联成功依据。
- 若必须确保某图出现在目标文件夹，非插件环境优先重新导入一个明确命名的文件副本到目标 `folderId`，再验证。
- 批量生成插件或脚本导入 Eagle 时，`annotation` 是提示词追溯的权威位置；不得只把 prompt 留在本地 report、控制台日志或插件任务面板。

### 删除规则

- 默认不删除 Eagle 内容。
- 删除文件夹、删除 item、直接改 Eagle library JSON，必须先给出路径、ID、影响范围、备份方案，并得到卡卡明确确认。
- 如果 Eagle API 不支持某类删除，也不能擅自直接改库文件；必须先停下来说明风险。
